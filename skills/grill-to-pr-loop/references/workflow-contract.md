# Deprecated Shim

This file is retained for older agents that still look for the former workflow router. It is not part of the default read set.

Load `references/core.md` for lifecycle, ownership, gates, local-first rules, and remote approval boundaries. Then use `context-contract.toml` as the single source of truth for operation-specific read sets.

If this file disagrees with `context-contract.toml`, the contract file wins.
